# Project Rules & Instructions

- **Logo & Background Protection**: NEVER change or modify the logo (`/image2.png` in `Gran7Logo` component) or background image configuration (`/logogran7.png`). These visual assets and their file paths/styles are locked and must be preserved as configured in `Layout.tsx` and `Login.tsx`.
